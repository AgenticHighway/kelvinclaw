# Kelvin OpenAI Model Plugin

First-party OpenAI model plugin for KelvinClaw.

Uses the OpenAI Responses API (`v1/responses`) with `gpt-4.1-mini` by default.

## Requirements

- `OPENAI_API_KEY` environment variable

## Build

```bash
chmod +x build.sh && ./build.sh
```
